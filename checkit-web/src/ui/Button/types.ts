export enum ButtonType {
  'SUBMIT' = 'submit',
  'BUTTON' = 'button',
  'RESET' = 'reset',
}

export enum ButtonStyle {
  'FILLED' = 'filled',
  'OUTLINE' = 'outline',
  'TEXT' = 'text',
  'DASHED' = 'dashed',
}

export enum ButtonShape {
  'SQUARE' = 'square',
  'CIRCLE' = 'circle',
  'ROUND' = 'round',
  'DEFAULT' = 'default',
}
